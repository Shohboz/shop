<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Электронная почта" %><br />
    <%= f.email_field :email %>
  </div>

  <br />

  <div class="actions">
    <%= link_to "Сохранить", "", class: "btn btn-large red accent-2 waves-effect waves-light", onclick: "$('form').submit()" %>
    <%= f.submit nil, style: "display: none;" %>
  </div>
<% end %>